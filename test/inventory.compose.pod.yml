---
all:
  children:
    dsm_source:
      hosts:
        rhds11:
          ansible_connection: containers.podman.podman
          ansible_host: rhds11
          ansible_user: root
          ansible_remote_tmp: /tmp/.ansible
          ansible_python_interpreter: auto_silent
          ansible_become: false
          # ds tools in container live in PATH, no SSH needed
    dsm_target:
      hosts:
        rhds12:
          ansible_connection: containers.podman.podman
          ansible_host: rhds12
          ansible_user: root
          ansible_remote_tmp: /tmp/.ansible
          ansible_python_interpreter: auto_silent
          ansible_become: false
